# 源站隐匿 — 开发模式：允许所有来源（本地 Docker / 直连调试）
# 生产环境请挂载 origin-shield.prod.conf 覆盖此文件

geo $realip_remote_addr $origin_shield_allowed {
    # $realip_remote_addr = 直连来源 IP (Cloudflare 回源 IP)
    default 1;
    127.0.0.1 1;
    ::1       1;
}
